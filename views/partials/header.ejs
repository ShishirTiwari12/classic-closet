<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&icon_names=search" />
<header>
    <a href="/"><h2>CLASSICCLOSET</h2></a>
    <div class="search">
      <input type="search" placeholder="Search products">
      <span class="material-symbols-outlined searchIcon">search</span>
    </div>
    <div class="menu">
        <a href="/"><p class="menuItem">Home</p></a>
        <a href="/wishlist" ><p class="menuItem" id="wishlist">Wishlist</p></a>
        <a href="/cart"><p class="menuItem" id="cart">Cart</p></a>
         <% if(!user) { %>
         <a href="/auth.html"><button id="auth">Login</button></a>
         <% } else { %>
          <a href="/order" ><p class="menuItem" >Orders</p></a>
          <a href="/profile"><p class="menuItem" style="color: orangered;"> <%= user.username %></p></a>
        <% } %>
    </div>
  </header>
  <script>
    const searchButton = document.querySelector('span');
    const input =document.querySelector('input');
    searchButton.addEventListener('click',search);

    function search(e){
      const value = input.value.trim();
      if(value==''){

      }
      else{
      window.location.href = `/search?q=${value}`;
      }
    }
  </script>